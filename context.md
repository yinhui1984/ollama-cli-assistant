# Context Instructions

- 使用 "$RPC_ETH" 作为 eth mainnet rpc
- 使用 "$APIKEY_DEEPSEEK" 作为 deepseek 的 API key

# Optional Notes

- 这里写长期有效的偏好与约束，不要写一次性的任务。
- 可以继续用上面的句式扩展，例如：
- 使用 "$RPC_BSC" 作为 bsc mainnet rpc
